# Copy to infra/minio/.env and replace credentials before starting MinIO.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=replace-with-a-long-random-password
MINIO_API_PORT=9020
MINIO_CONSOLE_PORT=9021
MINIO_DATA_DIR=../../minio-data
