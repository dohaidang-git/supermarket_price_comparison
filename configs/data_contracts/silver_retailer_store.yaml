name: silver_retailer_store
version: 1
grain: one retailer store context observed in one source run
record_key: retailer_store_id
fields:
  retailer_store_id: {type: string, required: true}
  retailer_id: {type: string, required: true}
  store_code: {type: string, required: true}
  store_group_code: {type: string, required: false}
  store_name: {type: string, required: false}
  region: {type: string, required: false}
  address_context: {type: string, required: false}
  source_run_id: {type: string, required: true}
  source_bronze_record_key: {type: string, required: true}
  built_at: {type: timestamp, required: true}
